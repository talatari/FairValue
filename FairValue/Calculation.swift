//
//  Calculation.swift
//  FairValue
//
//  Created by Tsaplin-SO on 10.09.2021.
//

import Foundation



func calcFairValue() {
    /*
    let betaParameterValue = betaParameter.text
    let divParameterValue = divParameter.text
    
    // параметр - базовый
    let base = baseField.text
    // параметр - риск премия
    let riskPrem = riskPremField.text
    // параметр - инфляция
    let infl = inflField.text
    // константа для проверки ограничения ввода параметра Бета
    //let constraintBeta = 0.3
    
    //let beta_ru = Double(betaParameterValue!)! * riskPrem + base
    //let ocenka_ru = (Double(divParameterValue!)! / (beta_ru - infl)) * 100

    let ocenka_ru = (Double(divParameterValue!)! / ((Double(betaParameterValue!)! * Double(riskPrem!)! + Double(base!)!) - Double(infl!)!)) * 100
    
    ocenka.text = String(round(ocenka_ru * 100) / 100)
    
    UserDefaults.standard.set(false, forKey: "stateSwitchInformation")
    
    print(String(UserDefaults.standard.bool(forKey: "stateSwitchInformation")))
    */
}
